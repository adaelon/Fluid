# Fluid 代码链路(改动账本)

> 每个切片(A1)完成时当场追加一条。每条精确到 `文件:符号`。

## 2026-06-14 S1 · 后端骨架 + L0 文件树

**触达**:
- `Cargo.toml` — 新增 workspace,成员 `crates/fluid-server`。
- `crates/fluid-server/Cargo.toml` — 后端 crate,bin 名 `fluid`;依赖 axum/tokio/serde/clap/walkdir/anyhow。
- `crates/fluid-server/src/main.rs:main` — clap 解析 `fluid <project> [--port]`,建 `ProjectReader`,绑 `127.0.0.1`,`axum::serve`。
- `crates/fluid-server/src/project_reader.rs:ProjectReader::list_files` — walkdir 递归列文件,跳 `SKIP_DIRS` 噪声目录,出扁平 `FileNode{path,name,lang}`。
- `crates/fluid-server/src/project_reader.rs:ProjectReader::read_file` / `:resolve` — 单文件读 + 路径穿越防护(拒绝绝对路径/`..`/canonicalize 越出 root),UTF-8 lossy 解码。
- `crates/fluid-server/src/routes.rs:router` — `GET /api/project/tree` → `{files}`;`GET /api/file?path=` → `{source}` / 403 / 404。
- `.gitignore` — 忽略 `target/`、`.fluid/`。

**入口**:`fluid <项目路径>` 起服务 → 浏览器/HTTP 调 `GET /api/project/tree`、`GET /api/file?path=<rel>`。

**测试**:
- `cargo test`(`project_reader::tests`,5 条):正常读 / 拒 `..` 穿越 / 拒绝对路径 / 缺失 404 / `list_files` 跳噪声目录。
- 端到端(确定性,B2):对 alphaGPT 起服务 → tree 返回 57 文件(33 py,= 知识图谱 33 个 file 节点)、`execution/config.py` 源码、`..` 穿越→403、缺失→404;对无 `.understand-anything/` 的 Fluid 仓库起服务→28 文件正常。

> 注:切片计划/需求文档写的"37 文件"是早期误估,真实为 33(详见本次会话决策摘要)。
