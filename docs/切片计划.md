# Fluid 切片计划

> 配套 `docs/技术方案.md`。每刀独立可验、依赖文件状态而非会话记忆(A4)。验证用确定性工具(B2)。
> 验证基准项目:`E:\allwork\download\agent\alphaGPT`(33 个 `.py`、57 个非噪声文件,Python;含 `.understand-anything/` 图谱 91 节点/199 边)。

## 状态:全部切片完成 ✅(截至 2026-06-17)

S1~S11(含 S7.5/7.6、S8、S9a/b、S10a/降级/追源/b/c、S11-a/b/c/d/lazy/md)、U-R1/U-R2、U1~U4、U5a/b/c、S-MD(Markdown 文档渲染视图)、S-MD-T(Markdown 文档翻译)、S-MD-T2(长文档分块并发,修 500)、S-MD-T3(翻译提速调参 + 流式进度/增量渲染) —— **全部已完成**(S-MD 已 push;S-MD-T/T2/T3 待 commit)。MVP 功能闭环:只读浏览、AST 解析、流式生成+缓存、追问器(分层降级+按需追源+跨文件破例)、手动单行补注、类 VSCode 壳(布局/tab/Open Folder/命令面板/设置面板)、视觉打磨。下方各切片标题保留历史轨迹。

## 切片依赖序

```
S1 后端骨架 ─┬─ S2 图谱加载
             └─ S3 前端壳+CM6 ── S4 tree-sitter 解析
S5 缓存 ─── S6 单函数生成(后端) ── S7 流式生成+渲染 ─┬─ S7.5 生成状态反馈 ── S7.6 失败归因+重试
                                                      ├─ S8 调度+大文件
                                                      └─ S9 手动单行(主)+gutter
S10 追问器 ── S11 视觉打磨(注释材质 ↔ IDE 壳一致性)
U-R 轨(代码阅读区,ADR-0016,优先): U-R1 右栏重构(暗色主题+行注释右栏+胶囊横头) ── U-R2 字体大小可调
U 轨(IDE 壳,ADR-0015): U1 IDE 布局 ── U2 文件 tab+面包屑 ── U3 Open Folder 切文件夹 ── U4 命令面板(可选)
U5 轨(设置面板,ADR-0018): U5a 后端运行时 LLM 配置 ── U5b 前端设置面板 ── U5c 测试连接(可选)
```

---

## S1 · 后端骨架 + L0 文件树
- **做**:Rust 后端(axum+tokio),`fluid ./project` 起服务;ProjectReader 列文件树、读源码;`GET /api/project/tree`、`GET /api/file`。
- **不做**:不碰图谱、不碰 LLM、不碰前端。
- **判据**:对 alphaGPT 起服务,`/api/project/tree` 返回文件树(57 文件,含 33 个 `.py`);`/api/file?path=execution/config.py` 返回源码;**无 `.understand-anything/` 也能起**。
- **触达**:`crates/fluid-server/`(`main.rs`、`project_reader.rs`、`routes.rs`、`Cargo.toml`)。

## S2 · 图谱加载(可选 + UTF-8 解码,GBK 兜底)
- **做**:GraphLoader 读 `knowledge-graph.json`,正确解码中文 summary,提供 nodes/edges 查询;`GET /api/project/graph`。
- **不做**:不改前端;不依赖图谱存在。
- **判据**:对 alphaGPT 解析出 91 节点/199 边,summary 中文不乱码;删除该目录后接口返回 `null` 且服务不崩(ADR-0011)。
- **触达**:`crates/fluid-server/src/graph_loader.rs`。

## S3 · 前端壳 + CM6 只读 + 文件树导航
- **做**:浏览器前端;文件树侧栏;点击文件 → CM6 只读渲染源码(真空态:gutter 无指示点)。
- **不做**:不解析、不生成、不显影。
- **判据**:浏览器打开,点 alphaGPT 任一文件,CM6 只读显示源码;追问器置灰显示"打开文件以启用追问"。
- **触达**:`web/`(Vue 3 + Vite,ADR-0014)、`web/src/FileTree.vue`、`web/src/Editor.vue`(CM6 包 `shallowRef`)。

## S4 · tree-sitter 解析(Python + Rust)
- **做**:tree-sitter WASM;对源码出 roster(FunctionSpan[])、lineRange、重点行 query。
- **不做**:不接 LLM;重点行规则先给 Python 一版、Rust 一版基础规则。
- **判据**:对 `execution/config.py` 与一个 `.rs` 样本,控制台输出函数清单 + 重点行行号,与人工核对一致。
- **触达**:`web/parser/`、`web/parser/keyline-queries/{python,rust}.scm`。
- **PENDING 消化**:重点行 query 具体规则在此刀定稿。

## S5 · 缓存(.fluid/,函数 span hash + 模型版本)
- **做**:CacheStore;`put/get`;键 = `hash(fnSpan)+modelVersion+promptVersion`(技术方案 §6)。
- **不做**:不接 LLM(用假数据测往返)。
- **判据**:put 后 get 命中且不调下游;改 fnSpan → miss;写入落在 `.fluid/`,源码不变。
- **触达**:`crates/fluid-server/src/cache_store.rs`。

## S6 · 单函数生成(后端,非流式)
- **做**:LLMProxy(持 key,模型 = opencode zen 网关的 **glm-5.1**,OpenAI 兼容)+ ContextAssembler;`/api/generate` 先做"整段返回";接 S5 缓存。
- **不做**:暂不流式、不前端渲染。
- **判据**:对 alphaGPT 某函数返回 Capsule + 重点行注释 JSON(中文、合理);**二次调用命中缓存零 token**(日志断言无 LLM 调用)。
- **触达**:`crates/fluid-server/src/{llm_proxy,context_assembler,routes}.rs`。

## S7 · 流式生成 + 前端渲染(显影)
- **做**:`/api/generate` 改 WS 流式(capsule→lines);前端 GhostStore + CM6 block widget 渲染胶囊/行级;§7.2/7.3 显影动画;折叠/展开纯 UI。
- **不做**:暂不做调度排序(顺序生成即可)、暂不大文件降级。
- **判据**:打开文件,函数胶囊逐个滑出、重点行卡片显影;折叠再展开**零重算**(日志断言);关文件释放内存、重开命中缓存秒显。
- **触达**:`web/ghostStore.ts`、`web/render/capsuleWidget.ts`、`web/render/lineWidget.ts`。

## S7.5 · 生成状态反馈(已完成 2026-06-15)
- **做**:函数级"生成中"骨架占位(`capsule` 帧到即替换)+ 编辑区全局进度 chip(`done/total`,基于每函数 `done`/`error` 帧计数),完成态短暂提示后淡出。
- **不做**:不动后端;不做 gutter 指示点(S9);不做 IDE 状态栏(U1,chip 是临时落点);不做调度并发(S8)。
- **判据**:打开 py/rs 文件即见每函数"生成中"骨架 + "生成中 x/N";capsule 到达替换真卡片、计数前进;全部完成"✓ N 已显影"并淡出。**npm run build 绿**(详见 `代码链路.md` S7.5)。
- **触达**:`web/src/render/placeholderWidget.ts`、`ghostStore.ts`、`render/ghostField.ts`、`Editor.vue`、`styles.css`。
- **源起**:用户反馈"生成静默无提示,用户很懵"。

## S7.6 · 失败归因 + 重试(已完成 2026-06-15)
- **做**:后端两个错误分支加日志(归因可见:`LLM error` 网关 / `LLM parse error` 坏 JSON);前端失败处显示「· 生成失败」+ 原因 tooltip + 「重试」按钮;`retry(fnId)` 经现有 WS 重发单函数(socket 断则重连补发 pending)。
- **不做**:不做自动重试/退避(坏 JSON 会无限烧 token);不加请求超时(reqwest 仍无 timeout,真挂起停在"生成中",留后续)。
- **判据**:`cargo test` 31/31、`npm run build` 绿;失败处出现「重试」点击重发(详见 `代码链路.md` S7.6)。
- **触达**:`crates/fluid-server/src/routes.rs`、`web/src/render/{placeholderWidget,ghostField}.ts`、`ghostStore.ts`、`Editor.vue`、`styles.css`。
- **源起**:用户问"为什么有些函数失败" + "给失败处加重试"。

## S8 · 生成调度(视口邻近排序 + 有限并发 + 滚动重排)(已完成 2026-06-15)
- **做**:GenScheduler:视口邻近排序、有限并发(N 条并行 WS,默认 4)、滚动重排。**整文件所有函数最终必生成**。
- **不做**(§0 收窄):**不做大文件视口门控**(放弃"超阈值只生成视口批/占位'滚动到此处自动生成'"——会重新引入 CONTEXT 已废弃的「视口激活」,与"打开即整体生成"铁律冲突);故大文件阈值 PENDING 一并取消。不动后端/WS 协议/渲染。
- **判据**:`scheduler-check.ts` 纯核断言全过(视口距离 + 队列升序 + 滚动重排 + retry 置顶);`npm run build` 绿。视口顺序派发 + 真并发 + 滚动重排留浏览器眼验(A2)。
- **触达**:`web/src/scheduler.ts`、`web/src/Editor.vue`、`web/scripts/scheduler-check.ts`。
- **决策见 `代码链路.md` S8**(并发载体 = 前端 N 条并行 WS;§0 放弃门控)。

## S9 · 手动单行补(主)+ gutter 指示(已完成 2026-06-15:S9a 后端 + S9b 前端)

> **范围重定(2026-06-15)**:S8 已保证「整文件所有**函数** + 其重点行」全量生成,S9 不再涉及任何**自动**生成;**主体 = 非重点行的手动按需补注**(CONTEXT「手动补行」,反馈#2 正解)。gutter 指示按 ADR-0016「行注释已默认可见」现状重定,降为发现性入口。"S8 全量 = 函数级,S9 手动 = 行级",二者正交,S8 未吃掉 S9。

- **做(主)**:非重点行 hover → 行尾/gutter 出「解释这一行」热区 → 点击 → `POST /api/explain-line {filePath, fn, lineNumber}` → 单行幽灵注释流式显影(同 §7.3 尾随式材质);结果入旁路缓存(键含行号),重开命中零 token。
- **做(次)**:gutter 发现性指示 —— 函数顶 2px 空心微点(§7.2);非重点行 hover 时出现可点热区。
- **不做**:**不做重点行实心脉动点**(ADR-0016 后行注释默认可见,脉动点冗余 → 砍;实做时同步改 §7.3);不动 S8 的函数级全量生成;不做"自动标注每一行"(违 CONTEXT 重点行 _Avoid_「每一行」)。
- **判据**:`cargo test` 全绿(explain-line handler 单元 + 行级缓存往返断言);非重点行 hover 出热区、点击触发单行生成并尾随显影;同一行二次触发命中缓存零 token(日志断言);重点行 / import / 结构行**不出**手动入口(前者已自动标注,后者刻意跳过)。
- **触达**:`web/src/render/gutter.ts`(新,空心点 + hover 热区)、`web/src/Editor.vue`(hover → explain 接线)、`crates/fluid-server/src/routes.rs`(`explain_line` handler + 路由)、`context_assembler.rs`(单行 prompt)、`cache_store.rs`(行级键,实做定稿)。
- **PENDING 定稿**:① 行级缓存键是否纳入 `lineNumber`(单行 vs 整函数键的取舍);② 热区落点(行尾 inline vs gutter 点)。
- **源起**:反馈#2「只看到部分注释,想看别的行」;范围在 S8 全量函数生成 + ADR-0016 注释默认可见后重定(本次会话讨论)。

## S10 · 追问器(分层上下文 + 跨文件 + 临时取源)

> **拆分(2026-06-15,§0 对齐)**:S10 是大切片,按"后端先行 + 真 token 流式"(用户两轮选定)拆为四刀,顺延边界固定:
> **S10a 后端流式追问闭环(下一刀)** → **S10a-降级 超窗逐级降级** → **S10b 前端 QueryPanel + dock** → **S10c 临时跨文件取源破例**。
> 追问器领域语义恒为"整个当前文件"(CONTEXT 追问器 / ADR-0006);跨文件默认只注一句话摘要(ADR-0007),临时取源是破例(S10c)。

### S10a · 后端流式追问闭环(基础)
- **做**:① 扩 `LlmProxy` 支持真 token 流式(SSE,OpenAI 兼容 `stream:true`);② 新 WS 端点 `/api/query`(独立于 `/api/generate`,帧 = text delta);③ ADR-0006 分层上下文装配【默认档】:文件 summary + 全部胶囊摘要 + edges + focus(聚焦函数/行)源码 + 跨文件一句话摘要(ADR-0007 默认面)。
- **不做**:不做超窗降级(大文件先按默认档直装 → S10a-降级);不做临时跨文件取源(→ S10c);不动前端;不改 `/api/generate` 既有协议(新端点独立)。
- **判据**:`cargo test` 全绿(分层装配纯函数单测 + 流式帧序列单测);真实 WS 冒烟(沙箱禁出网→无 LLM 走 error 帧 + 装配单测覆盖,真 LLM 流式答案留眼验,A2 诚实标注)。
- **触达**:`crates/fluid-server/src/llm_proxy.rs`(流式方法)、`routes.rs`(`/api/query` WS handler + 帧)、`context_assembler.rs`(query 分层装配分支)。
- **传输形态决策**:真 token 流式(非一次性 POST)——追问答案长,流式避免干等(呼应 memory「生成需可见状态」);后端先行正好隔离 LlmProxy streaming 改造,前端 S10b 只管渲染。否决非流式 POST(长答案体验差)。reverse 成本 = 改 LlmProxy + 端点(中等),沿 S6/S7a 判例未开新 ADR。

### S10a-降级 · 追问超窗逐级降级(已完成 2026-06-15)
- **做**:ADR-0006 降级阶梯 —— 全胶囊摘要之和仍超窗 → 按优先级(focus + 邻近向外)保留摘要、远端函数仅留名(清单行)。**字符代理预算**(`QUERY_CONTEXT_BUDGET_CHARS`,无 tokenizer 依赖)。
- **不做**(§0 拆分):**"模型按需追加某函数源码"的回退拆出去**(二次 round-trip,需改 WS 协议,留下一刀 S10a-追源);focus 源码不截断(单函数 span 实际有界)。
- **判据**:`cargo test` **59/59 绿**(新增 3 条:默认档全摘要 / 超窗降级远端仅留名+装配≤预算+slack+降级提示 / 选择器邻近优先)。大文件真 LLM 端到端走降级留眼验(A2)。
- **触达**:`crates/fluid-server/src/context_assembler.rs`(`QueryFocus`/`QUERY_CONTEXT_BUDGET_CHARS`/`build_query_prompt` 降级 + `query_spine_chars`/`select_capsule_summaries`)、`routes.rs:prepare_query`(focus 携 name)。
- **决策见 `代码链路.md` S10a-降级**(弹性部分=胶囊摘要;字符代理度量;邻近=capsules 源序下标距离;append 回退拆刀)。

### S10a-追源 · 追问降级后按需追源(已完成 2026-06-15)
- **做**:ADR-0006 回退面 —— 上下文降级后,模型两段式按需追源:phase-1 非流式规划调用点名(`{"need":[...]}`)→ 后端切出同文件函数源码(≤`QUERY_FETCH_BUDGET_CHARS`、去重、幻觉护栏)追加 → phase-2 流式作答。**门控仅降级时**;一轮不递归;失败/坏 JSON/点名不存在 → 退回降级答案。
- **不做**:跨文件取源(S10c);focus 截断;递归;tool-use/哨兵。
- **判据**:`cargo test` **69/69 绿**(新增 10:degraded 名单 / 切源+上界+护栏+去重 / 规划 prompt / extra 渲染 / parse_fetch_plan 容错)。`npm run build` 绿(rosterSpans 透传)。真两段式 round-trip 留眼验(A2)。
- **触达**:`context_assembler.rs`(`build_query_planning_prompt`/`query_degraded_names`/`slice_requested_sources`/`build_query_prompt` extra + `QUERY_FETCH_BUDGET_CHARS`)、`llm_proxy.rs:parse_fetch_plan`、`routes.rs`(`QueryPlan`/`prepare_query`/`run_query`/`QueryRequest.roster_spans`)、`web/src/{queryContext,api,QueryPanel}`。
- **决策见 ADR-0017 + `代码链路.md` S10a-追源**(两段式 vs tool-use/哨兵;门控;同文件;一轮不递归;CONTEXT 术语「按需追源」)。

### S10b · 前端 QueryPanel + dock(已完成 2026-06-15)
- **做**:QueryPanel 从真空壳改为可输入追问终端;接 `/api/query` WS 流式逐 token 显影;**dock 到底部面板**(用户选定,消化 ADR-0015/0016 右缘 PENDING)。
- **不做**:不注入 capsules/roster/focus(后端 optional + graph 兜底,留增强);答案先纯文本(markdown 渲染留后续)。
- **判据**:`npm run build` 绿(68 模块,CSS 7.37kB)。浏览器内追问流式答案 + 切文件清空 + 折叠留眼验(A2,此刻可端到端眼验 S10a 真流式)。
- **触达**:`web/src/QueryPanel.vue`、`web/src/api.ts`、`web/src/ghostTypes.ts`、`web/src/App.vue`、`web/src/styles.css`。
- **决策见 `代码链路.md` S10b**(dock=底部面板;只发 filePath+question)。
- **S10b-cap 增强(已完成 2026-06-15)**:消化 PENDING① —— QueryPanel 现把当前文件 roster + 已生成胶囊摘要随请求带上(Editor `@context` emit push 快照 → App → QueryPanel)。focus 仍未注入(无 UI 选中信号,留后续)。决策见 `代码链路.md` S10b-cap。
- **S11-md 增强(已完成 2026-06-15)**:消化 PENDING② —— 答案 `done` 后渲为 Markdown(markdown-it)+ LaTeX 公式(KaTeX)+ DOMPurify 消毒;流式期间仍纯文本。代码块语法高亮 / bundle 懒加载留后续。决策见 ADR-0008 + `代码链路.md` S11-md。
- **S10b-toggle 修订(已完成 2026-06-16)**:追问器默认隐藏(0 占位、空间还给代码区),改由状态栏「💬 追问」按钮开关(面板内 `✕` 可关);源自用户「一直展开太占位置」反馈。决策见 `代码链路.md` S10b-toggle。

### S10c · 临时跨文件取源破例(已完成 2026-06-16)
- **做**:ADR-0007 破例 —— 用户追问跨文件被调函数实现 → 复用 S10a-追源 两段式,把 graph 可定位的跨文件 callee 也列入 fetchable,模型点名 → 后端经 graph(file_path+line_range)切该函数 span、用完即弃(不打开/不缓存/不激活,目标文件事后仍真空)。
- **不做**(§0 收窄):不加前端(后端从 graph 自取 callee,零前端改);不改同文件追源逻辑;不做整文件拉取;不做前端 callee chip 交互(否决,工作量大)。
- **判据**:`cargo test` 新增确定性测试全绿(跨文件识别 + 切源 + 护栏:切源路径不创建 `.fluid`)+ clippy 无警告 + 前端 build 绿。✅ 74/74 绿;真 LLM 端到端留眼验。
- **触达**:`crates/fluid-server/src/{context_assembler,routes}.rs`(project_reader 无改,`read_file` 已够;后端自取 callee,前端无改)。
- **§0 对齐(2026-06-16)**:触发形态 = 扩展两段式规划(模型点名,非前端 chip);门控 = fetchable 非空即触发(非仅降级时)。两决策各触一个 ADR 边界 → ChangeType `[边界重构]`,§0.5 落档 ADR-0007/0017 修订。
- **决策见 ADR-0007 修订 + ADR-0017 修订 + `代码链路.md` S10c**(明确意图软化为模型点名;门控扩为 fetchable 非空;护栏结构化;名池不相交;锁内预读越锁)。
- **修复(2026-06-16,用户实跑回归)**:`cross_file_targets` 原只收 `function` 节点,但 understand-anything 把 Python 类实例化建模为指向 **`class` 节点**的 `calls` 边(图谱 class 40>function 14)→ 多数跨文件 callee 被丢弃,S10c 在真实项目基本失效。过滤放宽 `function | class`。详见 `代码链路.md`「S10c class 修复」。

### S11-lazy · 追问渲染依赖懒加载(性能,源自 S11-md 命门②;已完成 2026-06-16)
- **做**:把 QueryPanel 答案渲染用的 `katex`(含 auto-render + CSS + 字体)/ `markdown-it` / `dompurify` 从首屏主 chunk 移出,改为**首次渲染答案时动态 `import()` 按需加载**(Rollup 切独立 chunk,之后浏览器缓存)。纯性能优化,渲染功能/输出不变(同 ADR-0008)。
- **不做**:不改渲染行为与消毒链;不动后端;不碰 tree-sitter wasm(已是 `?url` 资产);不做代码块语法高亮(另一 PENDING⑦)。
- **判据**:`npm run build` 输出 katex/markdown-it 进**独立 chunk**、主 `index-*.js` 显著变小;打开页面 network 不请求 katex,首次追问 `done` 后按需拉取且 md+latex 正常渲染。浏览器 network 眼验留用户(A2)。
- **触达**:`web/src/QueryPanel.vue`(`renderAnswer` 内动态 import)、`web/src/render/markdown.ts`(markdown-it 改异步/整体懒引)、(可能)`web/vite.config.ts` manualChunks。
- **源起**:S11-md 引入三依赖致 JS bundle 1.2MB(`代码链路.md` S11-md 命门②;checkpoint PENDING⑧)。
- **结果**:主 `index.js` 1,205kB→765kB(−36%)、`index.css` 37→7.9kB;四库移入按需异步 chunk。详见 `代码链路.md` S11-lazy。

## S11 · 视觉打磨轨(美观提升)

> **范围重定(2026-06-16,本次会话讨论)**:原 S11 = 窄口「材质一致性 + §7.4 审计」,治不了用户提的"页面更美观"。审视现状发现两处 **intent ≠ delivery 硬伤** + 一处"最吵元素" + "身份缺失",故把 S11 重定为**视觉打磨轨**,按杠杆/成本拆为 a/b/c/d 四刀,每刀独立可验、不互相阻塞。原「§7.4 材质审计」折入 S11-b。
>
> **约束分层(§7.4)**:代码区内幽灵注释(胶囊/行注释)的美化必须守 §7.4(玻璃材质、无圆角、无阴影);壳层 chrome(状态栏/tab/侧栏/活动栏)不受 §7.4 限,可用圆角/阴影/图标自由发挥。
>
> **诊断依据(现状代码)**:① `styles.css:24` / `theme.ts:17` 声明 Inter / JetBrains Mono 但全仓库无 `@font-face`/webfont 引入 → 字体从未生效,现为 system-ui/Consolas;② `styles.css:521/552` 玻璃注释只有 `rgba()` 半透明、**无 `backdrop-filter`** → "玻璃态"欠交付;③ `styles.css:218` 状态栏满饱和 `#58a6ff` 蓝是全页最吵元素;④ `styles.css:1-8` 调色板逐 hex 照抄 GitHub-dark、色温/hover 硬编码散落 → 无身份、难维护。

### S11-a · 字体交付(最高杠杆;已完成 2026-06-16)
- **做**:本地自托管 Inter(UI)+ JetBrains Mono(代码)woff2,加 `@font-face`,让 `styles.css`/`theme.ts` 既有 font-family 真正生效(本地后端架构,不依赖 CDN)。
- **不做**:不改任何布局/配色/组件;不引第三方字体 CDN;不动后端。
- **判据**:`npm run build` 绿;字体文件作 `?url`/public 资产产出;浏览器 DevTools 确认 `.cm-scroller` 用 JetBrains Mono、UI 用 Inter(眼验留用户,A2)。
- **触达**:`web/src/main.ts`(latin 子集字重 CSS import)、`web/package.json`(`@fontsource/*`)。font-family 声明零改(名字本就对)。
- **源起**:本次会话诊断 —— 设计想用的字体从未加载。
- **结果**:`@fontsource/{inter,jetbrains-mono}` latin-only(Inter 400/600、JBM 400/700);font 资产产出、`index.css` 7.93→9.29kB、主 JS 不变。命门:`var(--mono)` 未定义留 S11-c。详见 `代码链路.md` S11-a。

### S11-b · 玻璃材质 + 状态栏降噪(含原 §7.4 审计;已完成 2026-06-16)
- **做**:① 给胶囊/行注释补 `backdrop-filter: blur()` + 极淡顶部高光,交付真·磨砂玻璃(守 §7.4:仍无圆角/无阴影);② 状态栏满饱和蓝改 `--panel` 底 + muted 字、accent 只点 active;③ 顺带原 §7.4 材质一致性核对(玻璃管线统一、色温竖线、折叠 4px 边线、文件切换 <150ms 过渡)。
- **不做**:不动布局结构;不改调色板 token(S11-c);不碰字体(S11-a)。
- **判据**:`npm run build` 绿;胶囊与行注释同一玻璃材质、无圆角/阴影/字体差异;状态栏不再是满屏蓝。浏览器眼验玻璃观感/状态栏降噪(A2)。
- **触达**:`web/src/styles.css`(`.fluid-capsule`/`.fluid-line-anno`/`.fluid-placeholder`/`.status-bar`/`.status-query-toggle`)。render/* 未改(类名已就位)。
- **源起**:本次会话诊断 —— 玻璃态欠交付 + 状态栏过吵;并入原 S11 材质审计。
- **结果**:三者统一 `gradient + backdrop-filter:blur(8px)` 玻璃(高光用 gradient 非 box-shadow 守 §7.4);状态栏 panel 底、accent 只点 done/active;`index.css` 9.29→9.67kB。详见 `代码链路.md` S11-b。

### S11-c · 设计 token + 身份色(已完成 2026-06-16)
- **做**:把调色板、语义色温(#7ee787/#f0883e/#ff7b72)、hover/active 透明度收进命名 CSS 变量(`--c-normal/--c-branch/--c-error`、`--hover`、`--active` 等);据选定方向微调身份色。
- **不做**:不做大改版;不动字体/玻璃(前两刀)。
- **判据**:`npm run build` 绿;散落硬编码 hex/rgba 收敛为 token;切主题色仅改 `:root`。眼验整体调性(A2)。
- **触达**:`web/src/styles.css`(`:root` + 全量引用)、`web/src/theme.ts`。
- **身份色决策(已定调)**:用户四选项中选 **「深空底 `#0b0e14` + 延续蓝标志 `#58a6ff`」**(`--panel #11161f`/`--border #232a35`)。否决青/紫标志、否决纯保持 GitHub-dark。语义色温 token 化但值不变。
- **源起**:本次会话诊断 —— 无身份、token 缺失。
- **结果**:`:root` 扩全套 token(身份色/hover/active/btn/accent-soft/语义色温/glass/mono/on-accent),散落硬编码全替换为 token(`#0d1117`/`#1f1f1f` 等);`--mono` 定义消化 PENDING⑯(答案代码块用上 JBM);theme.ts BG 同步 `#0b0e14`。`index.css` 9.67→10.23kB。详见 `代码链路.md` S11-c。

### S11-d · 细节打磨(已完成 2026-06-16)
- **做**:输入框 accent focus 环;自定义细滚动条;emoji(💬/✕)换内联 SVG 图标;chrome 层(tab/状态栏/侧栏/追问面板)加极淡阴影分层(§7.4 只禁代码区注释,chrome 不受限)。
- **不做**:不改信息架构;不动代码区 §7.4 玻璃注释的圆角/阴影禁令;`Tabs.vue` 的 `×`(纯符号非 emoji)有意未换(超点名范围)。
- **判据**:`npm run build` 绿;逐条对照(focus 环可见、滚动条细化、图标统一、chrome 分层)。眼验留用户(A2)。
- **触达**:`web/src/styles.css`(`:root` +3 token、滚动条/focus/阴影/图标对齐)、`web/src/shell/StatusBar.vue`(💬→SVG)、`web/src/QueryPanel.vue`(✕→SVG)。
- **源起**:本次会话诊断 —— 细节层逐条小改。
- **结果**:`:root` 加 `--scrollbar-thumb(+hover)`/`--shadow-chrome`;全局细滚动条 + 两输入框 accent focus 环 + 四 chrome 层方向性极淡阴影 + 两 emoji 换内联 SVG;`index.css` 10.23→11.45kB。**S11 视觉打磨轨 a/b/c/d 四刀全部完成**。详见 `代码链路.md` S11-d。

---

## U-R 轨 · 代码阅读区(ADR-0016,优先于 U 轨)

> 用户重定 U 轨优先级:先治「代码阅读区的美观与可读性」,再做外层 IDE chrome(U1~U4)。
> 行级注释从行间块卡片改为**右侧栏对齐**、函数胶囊改**单行横头**、补暗色主题、字体可调。代码区注释玻璃态(§7.4)不变。

### U-R1 · 代码阅读区右栏重构(已完成 2026-06-15)
- **做**:① 为 CM6 挂匹配 `#0d1117` 的暗色主题/语法高亮;② 行级注释从"行上方块卡片"改为"右侧栏 inline widget、与对应行 1:1 对齐、行高随注释增长、左侧色温竖线";③ 函数胶囊从多字段卡片改为定义行上方**单行横头**(`name · 摘要 · 复杂度`);④ 显影动画改为从右淡入+微滑。
- **不做**:不做拖宽右栏(固定一档宽)、不正式解决追问器右缘碰撞(浮于 rail 之上)、不做 IDE chrome(U1~U4)、不做 S8/S9、不做字体可调(U-R2)。
- **判据**:`npm run build` 绿;打开 py/rs 文件,代码列纵向不被打断、暗色高亮、行注释在右栏对齐且全文可见、胶囊为单行横头。浏览器内对齐/手感留眼验(A2,沙箱无浏览器)。
- **触达**:`web/src/render/{capsuleWidget,lineWidget,ghostField}.ts`、`web/src/Editor.vue`(暗色主题扩展)、`web/src/styles.css`、(可能新增)`web/src/theme.ts`。
- **PENDING 新增**:追问器(QueryPanel)与右栏右缘归属 → 标 PENDING,S10 实做追问器时 dock 到 rail 底。

### U-R2 · 字体大小可调(已完成 2026-06-15)
- **做**:代码阅读区字体大小可调(`Ctrl+=` 放大 / `Ctrl+-` 缩小 / `Ctrl+0` 复位,或状态栏控件);右栏注释随之等比缩放(§7.6);选择持久化(localStorage)。
- **不做**:不做行高/字族切换(留后续)、不影响后端。
- **判据**:`npm run build` 绿;快捷键改字号,代码与注释同步缩放,刷新后保持。眼验留用户。
- **触达**:`web/src/Editor.vue`、`web/src/styles.css`、(可能)`web/src/main.ts`(读持久值)。
- **源起**:用户反馈"字体大小需要可以改变"(2026-06-15)。

## U 轨 · 类 VSCode 界面壳(ADR-0015)

> 外层 IDE chrome 走标准视觉;代码区内注释保留玻璃态(§7.4 适用范围已收窄)。

### U1 · IDE 布局骨架(已完成 2026-06-15)
- **做**:活动栏 + 资源管理器侧栏 + 编辑区 + 底部状态栏;面板可拖宽;S7.5 进度 chip 迁入状态栏。
- **不做**:不做多 tab(U2)、不做切文件夹(U3)。
- **判据**:三区布局 + 状态栏出现;拖动分隔条改宽;进度显示在状态栏。
- **触达**:`web/src/App.vue`、新增 `web/src/shell/`(布局组件)、`styles.css`。

### U2 · 文件 tab + 面包屑(已完成 2026-06-15)
- **做**:多文件 tab(打开/激活/关闭)、路径面包屑;切 tab = 切激活文件(复用 Editor 激活链)。
- **判据**:开多个文件成多 tab,点 tab 切换,关 tab 释放;面包屑显示当前路径。
- **触达**:`web/src/shell/Tabs.vue`、`App.vue`。

### U3 · Open Folder 切文件夹(已完成 2026-06-15,含 U3-R 系统选择器修订)
- **做**:后端加"开文件夹"端点(运行时换根 / 多根)+ 前端选择器;切根后刷新文件树。
- **不做**:不做最近打开列表(后续)。
- **判据**:从 UI 选另一个目录 → 文件树切换 → 打开新目录文件正常生成;路径穿越防护对新根仍生效。
- **触达**:`crates/fluid-server/src/{routes,project_reader}.rs`、`web/src/api.ts`、`shell/`。
- **PENDING 消化**:换根 vs 多根的取舍在此刀定。

### U4 · 命令面板(已完成 2026-06-16)
- **做**:Ctrl+P 快速打开文件 / Ctrl+Shift+P 命令面板。
- **不做**(范围用户定):**不**做「折叠全部/重试全部失败」(需给 Editor 新增 foldAll/retryAll + defineExpose,耦合翻倍,留后续);命令仅列 App 层已接好的(设置/打开文件夹/切换追问器/关闭当前标签页)。
- **判据**:`node scripts/fuzzy-check.ts` 绿 + `npm run build` 绿;Ctrl+P 模糊找文件并打开(眼验)。
- **触达**:`web/src/shell/{fuzzy.ts,CommandPalette.vue}`(新)、`web/scripts/fuzzy-check.ts`(新)、`web/src/App.vue`、`web/src/styles.css`。
- **结果**:模糊匹配抽 `fuzzy.ts` 纯函数(`fuzzyMatch`/`fuzzyFilter`)+ `fuzzy-check.ts` 14/14 锁排序;`CommandPalette.vue` 父驱动 items(files/commands 两模式共用);App 全局 keydown Ctrl/Cmd+P(±Shift)+ preventDefault。`fuzzy-check` 14/14、build 绿(CSS 13.60→14.63kB、JS 772→776kB)。**U 轨 IDE 壳 U1~U4 全部完成。** 详见 `代码链路.md` U4。

> 原游离备忘「代码部分的页面需要优化」已收编为 **U-R 轨**(代码阅读区重构,ADR-0016)。

## U5 轨 · 设置面板(运行时配置 LLM 后端,ADR-0018)

> **§0 对齐(2026-06-16)**:用户要「加一个设置面板配置 LLM 后端」。术语澄清:与计划内 **U4=命令面板** 是两个功能,本功能另开 U5。四个分叉用户已定:① 可改 `base_url`/`model`/`api_key` 三项;② 持久化=回写 fluid 仓库 `.env` + 热更新内存代理;③ key **write-only**(GET 不回显,只给 masked 状态,空输入=保持原 key);④ 入口=活动栏底部齿轮 → 居中模态。RiskReceipt:用户知悉并接受「key 经本地 HTTP 进前端」,write-only 缓释。ChangeType `[边界重构]` → 落 ADR-0018。
>
> **后端 API 契约**:
> ```
> GET  /api/settings/llm
>   → 200 { baseUrl, model, keyStatus: "set"|"unset", keyHint: "···"+last4 | null }   // 永不返完整 key
> POST /api/settings/llm   { baseUrl, model, apiKey? }   // apiKey 空/缺 = 保持原值
>   → 200 { baseUrl, model, keyStatus, keyHint }
>   副作用(写锁内原子): 1. 重建 LlmProxy → AppState.llm  2. model 变→重建 cache 指针(镜像 open_folder)  3. 回写 .env 三行
> ```

### U5a · 后端运行时 LLM 配置(已完成 2026-06-16)
- **做**:`AppState.llm`/`model` 由不可变挪到锁后(沿 `project: RwLock` 模式);加 `GET`/`POST /api/settings/llm`(契约见上);`.env` 回写纯函数 `rewrite_env(old, &cfg) -> String`;热重建内存代理 + model 变更重建 cache 指针。
- **不做**:不支持非 OpenAI 兼容协议;不回显完整 key;不动生成/追问领域逻辑;不做前端(U5b);不做测试连接(U5c)。
- **判据**:`cargo test` 新增确定性绿(`rewrite_env` 更新已存行/追加缺失行/不动其他行;masked `keyHint` 末 4 位;POST 改 model 触发 cache 重建断言;空 apiKey 保持原 key)。真切换 provider 端到端留眼验(A2)。
- **触达**:`crates/fluid-server/src/settings.rs`(新)、`{routes,llm_proxy,main}.rs`(端点 + 锁化 + .env 回写)。
- **结果**:新建 `settings.rs`(`LlmConfig`/`mask_key`/`rewrite_env`);`AppState.llm`→`RwLock<LlmState{config,proxy:Option<Arc<LlmProxy>>}>` + `env_path`;四个 LLM 使用点改 Arc 快照越锁越 await;`apply_llm_settings` 可测核心(空 key 保持、model 变重建 cache)。`cargo test` **84/84**(+10)、clippy 无警告。详见 `代码链路.md` U5a。

### U5b · 前端设置面板(已完成 2026-06-16)
- **做**:`ActivityBar.vue` 底部加齿轮按钮;`web/src/shell/SettingsModal.vue`(居中模态 + 遮罩,表单 base_url/model/api_key);`App.vue` 加 `settingsOpen` ref 掌控开合;`api.ts` 加 `getLlmSettings()`/`saveLlmSettings(req)`。打开→GET 填充 + 显 keyHint(「已配置 ···last4」/「未配置」);apiKey input placeholder「留空＝保持不变」;保存→POST→刷新 masked + 内联「✓ 已保存」。
- **不做**:不做命令面板入口(U4);不回显完整 key;不做测试连接(U5c)。
- **判据**:`npm run build` 绿;浏览器内填写保存、write-only key、切 provider 立即生效留眼验(A2)。
- **触达**:`web/src/shell/{ActivityBar,SettingsModal}.vue`、`web/src/App.vue`、`web/src/api.ts`、`web/src/styles.css`。
- **结果**:齿轮 `margin-top:auto` 沉底 + 内联 SVG;`SettingsModal` GET 填充 / write-only key(type=password、起始空、placeholder 显 masked、保存后清空)/ Esc·遮罩·✕ 关闭;`api.ts` 加 `LlmSettings`+两函数。`index.css` 11.45→13.49kB、主 JS 766→771kB。**U5 轨 a+b 闭环;U5c 暂缓。** 详见 `代码链路.md` U5b。

### U5c · 测试连接(已完成 2026-06-16)
- **做**:`POST /api/settings/llm/test { baseUrl, model, apiKey? }` 用给定配置做一次最小 completion → 返回 `{ ok, error? }`;面板加「测试连接」按钮,保存前可验。
- **不做**:不自动重试;不持久化(纯验证,不落 .env);不改运行时代理。
- **判据**:`cargo test`/`npm run build` 绿;真端点返回 ok/err 做 B2 锚(非 AI 自评)。真连接留眼验。
- **触达**:`crates/fluid-server/src/routes.rs`(`test_llm_settings`/`resolve_test_key`)、`web/src/api.ts`(`testLlmSettings`)、`web/src/shell/SettingsModal.vue`、`web/src/styles.css`。
- **结果**:临时配置纯探针(用请求值构造代理,非运行时 proxy)、write-only key 解析抽 `resolve_test_key` 纯函数与 `apply_llm_settings` 共享并单测;`cargo test` **85/85**(+1)、clippy 净;前端 build 绿,`index.css` 13.49→13.60kB。**U5 轨 a+b+c 全部完成。** 详见 `代码链路.md` U5c。

---

## S-MD · Markdown 文档渲染视图(已完成 2026-06-17)
- **做**:打开 `.md`/`.markdown` 时在编辑区直接渲染为格式化文档(标题/列表/代码块/链接/表格 + KaTeX 公式),取代纯文本源码视图;复用追问答案的 `markdown-it→DOMPurify→KaTeX` 链(S11-lazy 动态 import,零新依赖)。后端 `lang_of` 加 `md` 标签,前端按 lang 切独立 `MarkdownView`(不污染 CM6 Editor),md 完全绕开生成 → 守真空态。
- **不做**(§0/§0.5 收窄):不做代码块语法高亮(素色,留后续刀);不做源码/预览切换或并排(用户选「直接渲染」);不做字号调节;不动 QueryPanel(共享渲染工具复用留后续重构刀);不支持 `.txt`/`.rst`;不解析相对路径图片/原始内嵌 HTML(PENDING)。
- **判据**:`cargo test` **91/91**(+`tags_markdown_as_md`)、clippy 净;`npm run build` 绿(渲染库仍在独立 async chunk)。浏览器内 md 渲染观感留眼验(A2)。
- **触达**:`crates/fluid-server/src/project_reader.rs`、`web/src/render/markdownDoc.ts`(新)、`web/src/MarkdownView.vue`(新)、`web/src/App.vue`、`web/src/styles.css`、`CONTEXT.md`(术语「文档渲染视图」)。
- **§0/§0.5**:`[模型扩展]`;NEW 术语「文档渲染视图」落 CONTEXT;未开 ADR(删组件即回退)。决策见 `代码链路.md` S-MD。
- **源起**:用户「加一个切片,做 md 文档的格式显示(不是生成注释,是 md 格式显示)」。

## S-MD-T · Markdown 文档翻译(英译中,已完成 2026-06-17)
- **做**:md 文档头部加 `[原文 | 译中文]` 切换;点译中文→后端整篇英译简中(`POST /api/translate`),译文写 `.fluid/` 旁路缓存、经 `markdownDoc` 链渲染、与原文原地切换。**代码块不翻译**:后端行扫描围栏块换占位符 `[[FLUID_CODE_BLOCK_n]]`、只把散文发 LLM、译后还原原块(代码字节不进模型)。重开未改 md 零 token。
- **不做**(§0/§0.5 收窄):不改源码、不写项目目录(只 `.fluid/`);不自动翻译(按钮触发);不并排;行内 code 靠 LLM 指令保留(非确定性);不做分段流式进度 / 长文档分块 / 占位符丢失模糊兜底(均 PENDING)。
- **判据**:`cargo test` **96/96**(+4 拆分/还原、+1 译文缓存往返)、clippy 净;`npm run build` 绿(渲染库仍独立 async chunk)。真 LLM 翻译质量/代码原样/原地切换留眼验(A2,沙箱禁网)。
- **触达**:`crates/fluid-server/src/translate.rs`(新)、`cache_store.rs`、`routes.rs`、`main.rs`、`web/src/api.ts`、`MarkdownView.vue`、`styles.css`、`CONTEXT.md`(术语「文档翻译」)。
- **§0/§0.5**:`[模型扩展]`(落点=旁路,不破"只写 .fluid/"铁律);NEW 术语「文档翻译」落 CONTEXT;未开 ADR(易反悔)。决策见 `代码链路.md` S-MD-T。
- **源起**:用户「再加一个切片,给 md 文档加英译中:整篇英文 md 变中文 md,不变源码只写文件,代码部分不翻译」。

## S-MD-T2 · 长文档分块并发翻译(修 500,已完成 2026-06-17)
- **做**:`run_translate` 由"整篇一次 complete"改为**分块有限并发**——`split_chunks`(2500 字符,空行边界、无损、不切结构)→ `buffered(2)` 保序并发,每块重试 2 次 + `tokio::timeout` 240s + 耗时日志(不动全局 reqwest client);坏块降级保留原文(英文)、全失败才整篇报错;`restore_code` 统一还原、整篇缓存。**并发/chunk/timeout 经二次实跑修订**(并发 4 撞共享网关排队超时 → 降 2)。
- **不做**:不动前端(端点契约不变);不分块缓存;不做分段进度;不全局改 reqwest timeout(只 translate 局部);单超长无空行段仍可能 500(PENDING)。
- **判据**:`cargo test` **100/100**(+4 `split_chunks` 无损/边界/超大块/占位符)、clippy 净。真 LLM 长文档(SKILL.md)不再 500、坏块降级留眼验(A2,沙箱禁网,正是修用户实跑的 500)。
- **触达**:`crates/fluid-server/src/translate.rs`(`split_chunks`)、`routes.rs`(`run_translate` 分块并发 + `translate_one_chunk`)。
- **源起**:用户实跑长 md「翻译了很久然后失败,LLM HTTP 500」,问能否 chunk 并发解决 → 诊断根因=单请求过大,chunk 治失败、并发提速、坏块降级。决策见 `代码链路.md` S-MD-T2。

## S-MD-T3 · 翻译提速(实跑调参)+ 流式进度/增量渲染(已完成 2026-06-17)
- **做**:① 据实跑日志(每块 36-122s、网关并行)调参——并发 2→4、chunk 2500→3500(提并发砍墙钟 ~11min→~6min、加大 chunk 减调用)。② `/api/translate` POST→**流式 WS**:每块译完即推 `chunk` 帧,前端 join 增量重渲 + 头部「翻译中 N/total 段」进度,把几分钟空白等待变"边译边读"。
- **不做**:不改模型速度(单次延迟是物理下限,治本=换更快模型,设置面板非代码);不做 token 级进度(块级);不做分块缓存;`buffered` 保序(后块快于前块仍排队等前块发出)。
- **判据**:`cargo test` **101/101**(+1 帧序列化)、clippy 净;`npm run build` 绿(渲染库仍独立 async chunk)。墙钟提速 + 逐段显影 + 进度 + 切原文不断流留眼验(A2,沙箱禁网,重跑 SKILL.md)。
- **触达**:`crates/fluid-server/src/routes.rs`(调参 + `TranslateFrame`/`translate_ws`/`run_translate_stream`)、`web/src/api.ts`(`streamTranslate`)、`MarkdownView.vue`(流式+进度+增量)、`styles.css`。
- **源起**:用户实跑日志显示分块后不再 500 但慢(每块~80s),问"还有优化空间吗" → 诊断瓶颈=模型单次延迟(代码改不了)但网关并行可提并发;用户选"调参提速 + 进度增量"两者都要。决策见 `代码链路.md` S-MD-T3。

---

## S-TS-1 · TS/JS 语法高亮(已完成 2026-06-18)
- **做**:给 `.ts/.tsx/.js/.jsx` 加 CM6 语法高亮 —— 后端 `lang_of` 打 flavor 标签、前端 `Editor.langExtension` 映射 `@codemirror/lang-javascript`。
- **不做**:不碰幽灵注释生成(`isParserLang` 仍只认 py/rs → ts 进 Editor 但只读不生成);不写 `typescript.scm`;不动 md 路由。
- **判据**:`cargo test` **102/102**(+`tags_ts_js_family_for_highlighting`)、clippy 净;`npm run build` 绿。浏览器内 `.ts` 彩色高亮留眼验。
- **触达**:`crates/fluid-server/src/project_reader.rs:lang_of`、`web/src/Editor.vue:langExtension`、`web/package.json`。详见 `代码链路.md` S-TS-1。
- **源起**:用户「为什么项目没有对 ts 文件的注释支持,语法高亮也没有」→ 诊断后拆两刀,本刀治高亮。

## S-TS-2 · TS 幽灵注释生成(已完成 2026-06-18)
- **做**:让 `.ts` 生成函数胶囊 + 重点行(纯前端 AST 接线;后端生成语言无关,零改)。`ParserLang +'ts'`、`browser.ts` 载 `typescript.wasm`、`ROSTER_QUERY['ts']`(函数声明/类方法/箭头·函数表达式·类字段箭头赋值)、新 `typescript.scm` 重点行 query、`isParserLang +'ts'`、`parse-check.ts` 加 TS 断言。
- **不做**:不做 tsx/jsx **生成**(JSX 重点行另议,刀一已给高亮);不改后端/prompt;对象字面量方法·IIFE 未纳入 roster(罕见,留后续)。
- **判据**:`node scripts/parse-check.ts` TS 断言全过(roster=5、重点行各归正确 host);`npm run build` 绿(`typescript.wasm` 作 `?url` 资产懒拉)。真 LLM 对 TS 的胶囊/行注释质量留眼验。
- **触达**:`web/src/parser/{types,parse,browser}.ts`、`web/src/parser/keyline-queries/typescript.scm`(新)、`web/src/Editor.vue:isParserLang`、`web/scripts/parse-check.ts`。ADR-0005 补 TS 后续记。详见 `代码链路.md` S-TS-2。
- **v1 已知局限**:箭头/函数表达式 const 的定义行同时是 roster 函数 + key line(冗余行注释,tree-sitter 无 not 谓词无法排除);标注于 `.scm`。
- **源起**:用户「做刀二」。

## S-TS-3 · TS 顶层 const/type 手动按需解释(已完成 2026-06-18)
- **做**:TS 模块顶层 `const/let/type/interface/enum` 提供"解释这个声明"手动入口(hover 热区 → 点击 → 按需生成行注释 + 行级缓存)。泛化 S9「解释这一行」到声明粒度,复用 explain-line(声明作退化 fn 传 + 声明措辞 prompt),纯按需不自动、守真空态。
- **不做**:不自动全量生成(中途从"自动声明胶囊"回退改为手动按需);不碰函数胶囊/重点行;只 `.ts`(tsx/js/jsx 只高亮);函数内声明已由 S9 覆盖,不重复。
- **判据**:`node scripts/parse-check.ts` 声明清单断言全过;`cargo test` **103/103**(+声明 prompt 测试)、clippy 净;`npm build` 绿。hover+点击+显影留眼验。
- **触达**:`web/src/parser/{types,parse}.ts`、`ghostStore.ts`、`render/{gutter,ghostField}.ts`、`Editor.vue`、`api.ts`、`scripts/parse-check.ts`;`crates/fluid-server/src/{context_assembler,routes}.rs`;`CONTEXT.md`。详见 `代码链路.md` S-TS-3。
- **§0/§0.5**:意图漂移(自动→手动)强制回退 §0 重签,ChangeType [边界重构]→[模型扩展];未开 ADR(删 query+入口即回退)。
- **源起**:用户「把 ts 中的 type 和 const 也加入注释行列」→ 澄清范围(顶层 vs 函数内)→ 改手动按需。

---

## 用户反馈 → 切片映射(2026-06-15)

用户看 S7 demo 后提 4 个问题,落为如下切片(决策见 ADR-0015):

| 反馈 | 解决方案 / 切片 | 状态 |
|---|---|---|
| #1 生成无任何提示,用户很懵 | **S7.5** 生成中骨架 + 进度 chip;**S8** 视口排序+并发更快;U1 进度迁状态栏 | S7.5 ✅ / S8 ✅ |
| #2 只看到部分注释,想看别的(行/函数) | 澄清:每个函数都生成胶囊,仅**重点行**自动注释;非重点行 → **S9** 手动「解释这一行」 | S9 ✅ |
| #3 界面太丑,想要类 VSCode | **U-R 轨**(代码阅读区右栏重构 + 字体可调,ADR-0016)→ **U 轨**(U1~U4 外层 chrome,ADR-0015);原 S11 重定义为注释材质 ↔ IDE 壳一致性核对 | U-R ✅ / U ✅ / S11 ✅ |
| #4 默认锁死 alphaGPT,要切文件夹 | **U3** Open Folder(后端换根端点 + 前端选择器) | U3 ✅ |

**附带**:#1 诊断时发现失败无归因 → 追加 **S7.6**(失败归因日志 + 重试按钮),已完成。
**附带(2026-06-15 追加)**:用户进一步明确"重点在代码阅读区美观与可读性"→ 拆 **U-R1**(右栏对齐 + 暗色主题 + 胶囊横头);并要求"字体大小可调"→ **U-R2**。决策见 **ADR-0016**;U-R 轨优先级高于 U 轨。

**排序决策(决策 2,用户定)**:先做 **S7.5 生成状态反馈**(快赢、改动小、立刻消除"懵"),再进 **U 轨**(界面焕新 + 收编 #3/#4),其后回到 S8/S9。**否决**:① 先做 U1 大重构(界面变化最大但起步重,用户的"懵"得拖);② 先做 S8 调度(治本但用户当下痛点是反馈缺失,非速度)。

---

## 验收锚点(全程,B2 确定性验证)
- 未打开文件调任意接口 → `INACTIVE_CONTEXT` 且无 LLM 调用(自动化断言)。
- 重开未变文件 → 缓存命中、零 token(日志断言)。
- 折叠/展开 → 零重算(日志断言)。
- 源码字节数/mtime 全程不变(零字节污染断言)。
