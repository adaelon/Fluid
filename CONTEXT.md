# Fluid

面向零代码基础用户的**只读**代码理解环境。在不修改源码任何字节的前提下,用 LLM 生成人类可读的语义投影,帮用户理解代码每一行的含义。

本文件只是术语表——定义每个名词"是什么",不含实现细节、不含决策理由(理由见 `docs/adr/`)。

## Language

**幽灵注释(Ghost Annotation)**:
LLM 生成的、仅存在于内存/旁路缓存、绝不回写源码的语义解释文本。源码对其他 Agent 的 Token 成本因此为零。
_Avoid_: 注释、comment(易与源码内真实注释混淆)、批注

**零字节污染(Zero Byte Contamination)**:
铁律——所有幽灵注释与缓存只写旁路文件,源码文件一个字节都不被改写。
_Avoid_: 只读、无侵入

**激活单元(Activation Unit)**:
Fluid 的原子激活/生成/上下文边界,**恒为单个文件**。未打开的文件是绝对真空(零数据、零 Token);打开一个文件即对该文件整体生成语义投影。
_Avoid_: 函数级激活、视口激活(本项目已废弃更细的激活粒度)

**真空态(Vacuum State)**:
文件未被打开时的状态——零数据、零调度、零渲染、零 Token。

**文档渲染视图(Document Render View)**:
打开 Markdown 文档(`.md`/`.markdown`)时,将其源码渲染为格式化排版(标题/列表/代码块/链接/表格…)直接呈现于编辑区,取代只读源码视图。属"文件本身格式的另一种呈现",**不是幽灵注释**——不经 LLM、不解析、不生成、不激活生成管线,md 文件因此保持真空态。守零字节污染(只渲染,不回写源码)。
_Avoid_: Markdown 注释(易与幽灵注释混淆)、md 预览面板(它是主视图非独立面板)

**文档翻译(Document Translation)**:
将打开的英文 Markdown 文档整篇翻译为简体中文的旁路产物。由用户在文档渲染视图里点「译中文」按钮触发,译文写入 `.fluid/`(不改源码,守零字节污染),渲染后与原文一键原地切换。**代码不翻译**——围栏代码块(```)与行内代码原样保留,只翻译自然语言散文。是"换一种语言看整篇文档",非逐行幽灵注释。
_Avoid_: 注释翻译、回写译文到源文件、翻译代码

**函数胶囊(Function Capsule)**:
函数粒度的语义聚合单元,含函数签名、职责摘要、复杂度、输入输出抽象。文件打开时全量生成,每个胶囊一个 LLM 请求。
_Avoid_: 函数卡片、函数节点

**重点行(Key Line)**:
由 AST 启发式判定为"有语义负载"的源码行(含计算的赋值、分支、调用、关键 return 等)。文件打开时只为重点行默认生成行级幽灵注释;import/装饰器/纯结构行跳过。
_Avoid_: 行原子(Line Atom,旧术语)、每一行

**手动补行(Manual Line Fill)**:
用户对一个未默认标注的非重点行,主动触发"解释这一行"的单行按需 LLM 生成。
**也适用于 TS 模块顶层声明**(const/let/type/interface/enum):这些声明不属于任何函数胶囊、不自动生成,用户可在声明行点"解释这个 const/type/…"按需生成单条注释(同一旁路缓存,重开零 Token)。是"按需补注"的声明粒度泛化,守真空态。
_Avoid_: 顶层声明自动注释、声明胶囊

**代码选区(Code Selection)**:
用户在当前代码阅读区内手动选择的一段非空、单行、连续源码文本。选区不要求与 AST 节点边界重合,可小至标识符、大至单行表达式。
_Avoid_: 文件选择、多行代码块、自动符号识别

**选区解释(Selection Explanation)**:
用户针对代码选区主动触发的幽灵注释,说明所选文本是什么以及它在当前代码语境中的作用,并携带证据状态。它的浮层展示是临时的,但结果可由旁路缓存复用;它不生成或替换行级幽灵注释。
_Avoid_: 手动补行、函数胶囊、通用术语词典

**选区浮层(Selection Explanation Popover)**:
锚定代码选区、用于呈现选区解释的临时界面。浮层关闭后不在代码区留下可见注释。
_Avoid_: 新页面、行尾幽灵注释、追问器

**函数清单(Function Roster)**:
某文件内全部函数的列表(名称 + 行范围),由 Fluid 自带 AST 解析得出。作为该文件每个函数胶囊生成时的共享上下文注入。
_Avoid_: 函数列表

**旁路缓存(Bypass Cache)**:
落盘存储已生成的函数胶囊、行级注释与选区解释的缓存文件,写在源码之外。缓存键随产物粒度包含其源码身份与模型版本;命中则零 Token 秒显,相关源码或模型变更则失效重算。
_Avoid_: 硬缓存、持久化

**追问器(Query Terminal / queryInline)**:
增量问答终端,不是独立会话管理器。默认面向当前激活文件;也可切换到用户显式选择的文件集关系上下文,并可调用供应商托管联网检索补充公开技术资料。答案是 Markdown 文本(可含 LaTeX 数学公式),流式期间逐 token 显纯文本、完成后由前端渲染为格式化视图;渲染只在答案面板,源码零字节不变。
_Avoid_: 聊天框、会话、chatbot

**文件集关系追问(Selected File Set Query)**:
用户显式选择多个文件后,围绕这些文件的职责、调用、依赖与关系进行追问的上下文模式。它依赖上下文图谱提供文件/符号摘要与关系边;不等同于打开或激活这些文件。
_Avoid_: 全项目追问、打开多个文件、整文件拼接

**文件选择模式(File Selection Mode)**:
追问器触发的临时 UI 状态,用于在文件树中显示文件选择控件。关闭该模式只隐藏选择控件,不清空已选文件集;清空是独立动作。
_Avoid_: 多选打开、关闭即清空

**临时跨文件取源(Ephemeral Cross-File Fetch)**:
用户通过追问或选区解释明确要求理解当前文件所引用的项目内符号时,系统只取目标声明或实现的源码片段、用完即弃的破例。不打开、不缓存、不激活目标文件。
_Avoid_: 跨文件打开、预加载

**第三方符号(Third-Party Symbol)**:
定义位于项目根目录之外、由项目依赖声明引入的代码符号。其身份包含所属依赖及项目实际使用的依赖版本。
_Avoid_: 项目内跨文件符号、仅凭名称猜测的库 API

**第三方证据解析(Third-Party Evidence Resolution)**:
为第三方符号寻找定义与说明的过程。系统可用依赖版本提示缩小目标范围,再调用供应商托管联网检索寻找公开技术资料;读取本机第三方源码是可选增强,不是该过程成立的前提。只有带可追溯来源的结果才视为已核验证据。
_Avoid_: 把 LLM 记忆当作已核验证据、无来源的 API 猜测

**依赖版本提示(Dependency Version Hint)**:
项目 manifest 或 lockfile 中声明或锁定的第三方包名与版本,用于约束第三方证据解析。它本身不是源码证据;缺失时不阻断联网检索。
_Avoid_: 本机依赖根目录、仅凭模型记忆猜版本、把版本声明当实现定义

**证据状态(Evidence Status)**:
选区解释或追问答案对其依据来源的显式标识,区分「项目源码」「网页有来源」「联网无来源」与「未核验」。
_Avoid_: 置信度评分、把未核验解释伪装成事实

**供应商托管联网检索(Provider-Hosted Web Search)**:
由 LLM 供应商在其服务端执行的公开网页搜索能力,供选区解释和追问器共享。搜索结果可以包含来源 URL,也可以只返回无来源的整理内容;后者必须显式标为「联网无来源」。
_Avoid_: Fluid 自建网页抓取、保证供应商一定返回引用、把无来源结果称为已核验

**受控自动检索(Controlled Automatic Retrieval)**:
当选区解释或追问器涉及第三方符号、最新外部资料或现有证据不足时,系统可直接调用供应商托管联网检索,不逐次预览或确认检索词,并允许用户预先显式关闭。检索意图规划应只产出公开包名、版本、符号路径与技术问题。
_Avoid_: 每次作答都联网、联网后再征求确认、主动把完整源码写进搜索词

**检索意图规划(Search Intent Planning)**:
在不开启联网工具的 LLM 调用中,将当前代码语境归一为公开检索请求的过程。系统不把原始源码直接附加到联网请求,但 v1 不对 LLM 产出的检索词提供语义级隐私保证。
_Avoid_: 确定性依赖解析、带源码联网搜索、最终解释生成

**公开检索请求(Public Search Query)**:
从代码选区、导入关系、依赖版本提示与上下文图谱中提炼出的公开技术检索描述,只含包名、版本、完整符号路径与技术问题。它是供应商托管联网检索唯一接收的业务输入,不含源码片段、私有文件名或项目内标识符。
_Avoid_: 原始用户问题、完整代码上下文、私有搜索词

**联网检索结果(Web Search Result)**:
供应商托管联网检索返回并用于生成解释或答案的内容。若带页面标题和 URL,界面应允许用户追溯;若不带来源,仍可使用但必须保持「联网无来源」状态。
_Avoid_: 默认伪造来源、把无来源结果升级为网页证据

**联网降级(Web Search Fallback)**:
检索意图规划失败、当前供应商不支持联网、检索超时或限流时,系统继续使用本地上下文作答,显式标记「未核验:联网失败」并允许重新生成。
_Avoid_: 整体作答失败、静默降级、把本地回答标成联网结果

**按需追源(On-Demand Source Fetch)**:
追问上下文因超窗降级(远端**同文件**函数仅留名)后,模型可"点名"需要哪些函数的源码,系统两段式补回:先规划调用让模型列出所需函数名,再切出其源码追加进上下文重答。只取同文件函数、有上界、一轮不递归;与跨文件的「临时跨文件取源」区分(后者跨文件、破例)。
_Avoid_: 工具调用、自动展开

**上下文图谱(Context Graph)**:
管理项目级骨架索引(文件树 + understand-anything 的 nodes/edges)与文件级语义实例化的模块。

**解释引擎(Explanation Engine)**:
执行 AST 解析、重点行判定、LLM 调度与语义投影编译的模块。

**understand-anything**:
外部上游工具。产出 `.understand-anything/knowledge-graph.json`,含 file/class/function/document 节点摘要与 edges(calls/imports/...)。函数节点稀疏(不覆盖全部函数)、无行级数据,Fluid 将其作为输入而非完整真相源。
